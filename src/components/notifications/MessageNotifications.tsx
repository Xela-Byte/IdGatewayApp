import {View, Text} from 'react-native';

type Props = {};

const MessageNotifications = (props: Props) => {
  return (
    <View>
      <Text>MessageNotifications</Text>
    </View>
  );
};

export default MessageNotifications;
